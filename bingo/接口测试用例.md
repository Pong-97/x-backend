# API 接口测试用例

## 📋 测试说明

本文档提供完整的接口测试用例，包含正常场景和异常场景，方便前端开发者理解接口行为。

**测试环境**：
- 本地：`http://localhost:3000`
- 生产：`https://lyaftwgtwyqr.sealosbja.site`

---

## 1️⃣ 用户模块测试

### 1.1 用户注册

#### 测试用例 1：正常注册
```http
POST /user/register
Content-Type: application/json

{
  "username": "testuser001",
  "password": "123456",
  "email": "test001@example.com"
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1001
  }
}
```

#### 测试用例 2：用户名已存在
```http
POST /user/register
Content-Type: application/json

{
  "username": "testuser001",
  "password": "123456",
  "email": "test002@example.com"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "用户名已存在",
  "data": null
}
```

#### 测试用例 3：邮箱已被使用
```http
POST /user/register
Content-Type: application/json

{
  "username": "testuser002",
  "password": "123456",
  "email": "test001@example.com"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "邮箱已被使用",
  "data": null
}
```

#### 测试用例 4：参数验证失败
```http
POST /user/register
Content-Type: application/json

{
  "username": "ab",
  "password": "123"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "用户名长度必须在3-20个字符之间",
  "data": null
}
```

---

### 1.2 用户登录

#### 测试用例 1：登录成功
```http
POST /user/login
Content-Type: application/json

{
  "username": "testuser001",
  "password": "123456"
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "userId": 1001,
      "username": "testuser001",
      "email": "test001@example.com",
      "phone": null,
      "avatar": null
    }
  }
}
```

**注意**：保存返回的 token，后续需要认证的接口都要使用。

#### 测试用例 2：用户名不存在
```http
POST /user/login
Content-Type: application/json

{
  "username": "nonexistuser",
  "password": "123456"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "用户名或密码错误",
  "data": null
}
```

#### 测试用例 3：密码错误
```http
POST /user/login
Content-Type: application/json

{
  "username": "testuser001",
  "password": "wrongpassword"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "用户名或密码错误",
  "data": null
}
```

---

### 1.3 获取用户信息

#### 测试用例 1：获取成功
```http
GET /user/info
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1001,
    "username": "testuser001",
    "email": "test001@example.com",
    "phone": "13800138000",
    "avatar": "https://example.com/avatar.jpg",
    "createdAt": "2024-01-01T00:00:00.000Z"
  }
}
```

#### 测试用例 2：未提供 token
```http
GET /user/info
```

**预期响应**：
```json
{
  "code": 401,
  "message": "未授权，请先登录",
  "data": null
}
```

#### 测试用例 3：token 无效
```http
GET /user/info
Authorization: Bearer invalid_token
```

**预期响应**：
```json
{
  "code": 401,
  "message": "Token无效",
  "data": null
}
```

---

### 1.4 更新用户信息

#### 测试用例 1：更新成功
```http
POST /user/update
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "email": "newemail@example.com",
  "phone": "13900139000",
  "avatar": "https://new-avatar.jpg"
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

## 2️⃣ 商品模块测试

### 2.1 获取商品列表

#### 测试用例 1：基础列表查询
```http
GET /product/list?page=1&pageSize=10
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "productId": 1001,
        "name": "iPhone 15 Pro",
        "price": 7999,
        "originalPrice": 8999,
        "image": "https://example.com/iphone.jpg",
        "description": "最新款iPhone",
        "stock": 100,
        "sales": 520,
        "categoryId": 1,
        "status": 1
      }
    ],
    "total": 50,
    "page": 1,
    "pageSize": 10,
    "totalPages": 5
  }
}
```

#### 测试用例 2：按分类筛选
```http
GET /product/list?page=1&pageSize=10&categoryId=1
```

#### 测试用例 3：按销量排序
```http
GET /product/list?page=1&pageSize=10&sortBy=sales&order=desc
```

#### 测试用例 4：按价格升序排序
```http
GET /product/list?page=1&pageSize=10&sortBy=price&order=asc
```

---

### 2.2 获取商品详情

#### 测试用例 1：获取成功
```http
GET /product/1001
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "productId": 1001,
    "name": "iPhone 15 Pro",
    "price": 7999,
    "originalPrice": 8999,
    "image": "https://example.com/iphone.jpg",
    "images": [
      "https://example.com/iphone1.jpg",
      "https://example.com/iphone2.jpg"
    ],
    "description": "最新款iPhone",
    "detail": "<p>详细描述</p>",
    "stock": 100,
    "sales": 520,
    "views": 1234,
    "categoryId": 1,
    "specs": [
      { "name": "颜色", "value": "深空黑" },
      { "name": "容量", "value": "256GB" }
    ],
    "status": 1
  }
}
```

#### 测试用例 2：商品不存在
```http
GET /product/99999
```

**预期响应**：
```json
{
  "code": 404,
  "message": "商品不存在",
  "data": null
}
```

---

### 2.3 搜索商品

#### 测试用例 1：搜索成功
```http
GET /product/search?keyword=iPhone&page=1&pageSize=10
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [...],
    "total": 15,
    "page": 1,
    "pageSize": 10
  }
}
```

#### 测试用例 2：无搜索结果
```http
GET /product/search?keyword=不存在的商品&page=1&pageSize=10
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "pageSize": 10
  }
}
```

---

## 3️⃣ 购物车模块测试

### 3.1 添加到购物车

#### 测试用例 1：添加成功
```http
POST /cart/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "productId": 1001,
  "quantity": 2
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "cartId": 1
  }
}
```

#### 测试用例 2：商品已在购物车（累加数量）
```http
POST /cart/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "productId": 1001,
  "quantity": 1
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "cartId": 1
  }
}
```

**注意**：数量会累加到之前的数量。

#### 测试用例 3：库存不足
```http
POST /cart/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "productId": 1001,
  "quantity": 999999
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "库存不足",
  "data": null
}
```

#### 测试用例 4：商品不存在
```http
POST /cart/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "productId": 99999,
  "quantity": 1
}
```

**预期响应**：
```json
{
  "code": 404,
  "message": "商品不存在",
  "data": null
}
```

---

### 3.2 获取购物车列表

#### 测试用例 1：获取成功
```http
GET /cart/list
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "cartId": 1,
      "productId": 1001,
      "productName": "iPhone 15 Pro",
      "productImage": "https://example.com/iphone.jpg",
      "price": 7999,
      "quantity": 2,
      "selected": true,
      "stock": 100
    }
  ]
}
```

---

### 3.3 更新购物车

#### 测试用例 1：更新数量
```http
POST /cart/update
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "cartId": 1,
  "quantity": 5
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

#### 测试用例 2：更新选中状态
```http
POST /cart/update
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "cartId": 1,
  "selected": false
}
```

#### 测试用例 3：超出库存
```http
POST /cart/update
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "cartId": 1,
  "quantity": 999999
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "库存不足",
  "data": null
}
```

---

### 3.4 删除购物车商品

#### 测试用例 1：删除成功
```http
DELETE /cart/delete/1
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

#### 测试用例 2：购物车项不存在
```http
DELETE /cart/delete/99999
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 404,
  "message": "购物车项不存在",
  "data": null
}
```

---

## 4️⃣ 订单模块测试

### 4.1 创建订单

#### 测试用例 1：创建成功
```http
POST /order/create
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "addressId": 1,
  "cartIds": [1, 2],
  "remark": "请尽快发货"
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "订单创建成功",
  "data": {
    "orderId": 10001,
    "orderNo": "20240101123456789",
    "totalAmount": 15998
  }
}
```

#### 测试用例 2：地址不存在
```http
POST /order/create
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "addressId": 99999,
  "cartIds": [1, 2]
}
```

**预期响应**：
```json
{
  "code": 404,
  "message": "地址不存在",
  "data": null
}
```

#### 测试用例 3：购物车为空
```http
POST /order/create
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "addressId": 1,
  "cartIds": []
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "购物车不能为空",
  "data": null
}
```

---

### 4.2 获取订单列表

#### 测试用例 1：获取所有订单
```http
GET /order/list?page=1&pageSize=10
Authorization: Bearer <your_token>
```

#### 测试用例 2：按状态筛选
```http
GET /order/list?status=0&page=1&pageSize=10
Authorization: Bearer <your_token>
```

**状态说明**：
- 0：待付款
- 1：待发货
- 2：待收货
- 3：已完成
- 4：已取消

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [
      {
        "orderId": 10001,
        "orderNo": "20240101123456789",
        "status": 0,
        "statusText": "待付款",
        "totalAmount": 15998,
        "products": [...],
        "address": {...},
        "createdAt": "2024-01-01T12:34:56.000Z"
      }
    ],
    "total": 20,
    "page": 1,
    "pageSize": 10
  }
}
```

---

### 4.3 获取订单详情

#### 测试用例 1：获取成功
```http
GET /order/10001
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "orderId": 10001,
    "orderNo": "20240101123456789",
    "status": 0,
    "statusText": "待付款",
    "totalAmount": 15998,
    "remark": "请尽快发货",
    "products": [...],
    "address": {...},
    "createdAt": "2024-01-01T12:34:56.000Z",
    "paidAt": null,
    "shippedAt": null,
    "completedAt": null
  }
}
```

#### 测试用例 2：订单不存在
```http
GET /order/99999
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 404,
  "message": "订单不存在",
  "data": null
}
```

---

### 4.4 取消订单

#### 测试用例 1：取消成功
```http
POST /order/cancel/10001
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "订单已取消",
  "data": null
}
```

#### 测试用例 2：订单状态不允许取消
```http
POST /order/cancel/10002
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 400,
  "message": "当前订单状态不允许取消",
  "data": null
}
```

**注意**：只有待付款状态的订单可以取消。

---

### 4.5 确认收货

#### 测试用例 1：确认成功
```http
POST /order/confirm/10001
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "确认收货成功",
  "data": null
}
```

#### 测试用例 2：订单状态不允许确认收货
```http
POST /order/confirm/10002
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 400,
  "message": "当前订单状态不允许确认收货",
  "data": null
}
```

**注意**：只有待收货状态的订单可以确认收货。

---

## 5️⃣ 地址模块测试

### 5.1 添加地址

#### 测试用例 1：添加成功
```http
POST /address/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "name": "张三",
  "phone": "13800138000",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "detail": "科技园南区XX路XX号",
  "isDefault": true
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "addressId": 1
  }
}
```

#### 测试用例 2：参数验证失败
```http
POST /address/add
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "name": "张三",
  "phone": "138001380"
}
```

**预期响应**：
```json
{
  "code": 400,
  "message": "手机号格式不正确",
  "data": null
}
```

---

### 5.2 获取地址列表

#### 测试用例 1：获取成功
```http
GET /address/list
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "addressId": 1,
      "name": "张三",
      "phone": "13800138000",
      "province": "广东省",
      "city": "深圳市",
      "district": "南山区",
      "detail": "科技园南区XX路XX号",
      "isDefault": true
    }
  ]
}
```

---

### 5.3 更新地址

#### 测试用例 1：更新成功
```http
POST /address/update/1
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "name": "李四",
  "phone": "13900139000"
}
```

**预期响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

### 5.4 设置默认地址

#### 测试用例 1：设置成功
```http
POST /address/setDefault/1
Authorization: Bearer <your_token>
```

**预期响应**：
```json
{
  "code": 200,
  "message": "设置成功",
  "data": null
}
```

**注意**：设置后，其他地址的 isDefault 会自动变为 false。

---

## 6️⃣ 其他接口测试

### 6.1 获取分类列表

```http
GET /category/list
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "categoryId": 1,
      "name": "手机数码",
      "icon": "https://example.com/icon1.png",
      "sort": 1
    }
  ]
}
```

---

### 6.2 获取首页数据

```http
GET /home
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "banners": [...],
    "categories": [...],
    "hotProducts": [...],
    "newProducts": [...]
  }
}
```

---

### 6.3 健康检查

```http
GET /health
```

**预期响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "ok",
    "timestamp": "2024-01-01T12:34:56.789Z"
  }
}
```

---

## 📊 测试流程建议

### 完整购物流程测试
```
1. 用户注册 → POST /user/register
2. 用户登录 → POST /user/login (获取token)
3. 浏览商品 → GET /product/list
4. 查看详情 → GET /product/1001
5. 添加购物车 → POST /cart/add
6. 查看购物车 → GET /cart/list
7. 添加地址 → POST /address/add
8. 创建订单 → POST /order/create
9. 查看订单 → GET /order/list
10. 确认收货 → POST /order/confirm/:id
```

---

## ✅ 测试检查清单

- [ ] 所有正常场景测试通过
- [ ] 所有异常场景返回正确错误信息
- [ ] Token 认证机制正常工作
- [ ] 分页功能正常
- [ ] 数据验证正确
- [ ] 业务逻辑符合预期

---

**测试愉快！** 🎯
